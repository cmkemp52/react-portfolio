import React, { Component } from "react";
import Styled from "styled-components";

const AboutDiv = Styled.div`
height: 100vh;
`;

export class about extends Component {
  render() {
    return <AboutDiv></AboutDiv>;
  }
}

export default about;
